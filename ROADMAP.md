# Roadmap

## TUI / UX

- [ ] Interface discovery with checkbox selection when adding interfaces to a dashboard
- [ ] Add identity picker interface for confiruing defaults, or host identities. 

## Dashboard / Polling

- [ ] Set custom bandwidth per interface (override discovered speed)
- [ ] Set interface descriptions (inherit from SNMP discovery by default, allow override)
